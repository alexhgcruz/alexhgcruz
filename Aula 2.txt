Lembrando a aula 1....

git init -> pasta vira um repositório

git add . "nome arquivo" ->

git config user.name "nome" / config user.email "email" -> add usário e add email

git commit -am "mensagem" -> mensagem no programa, sendo a = atualizações / m = mensagem

git status -> verifica o status


remote -> nuvem (remoto / longe) / local -> nosso arquivo / origin -> origem do arquivo

git push -u origin master -> mandando o arquivo para a nuvem, o -u é porque ainda nao existe na nuvem....

